#include <stdio.h>
/*
*main a program that prints the alphabet in lower case followed by
*a new line.
*Return: 0 (success)
*/
int main(void)
{
char x = 'a';
while ( x < 'z' )
{
putchar(x);
putchar('\n');
a++;
}
return (0);
}
